import { ComingSoon } from "@/components/dashboard/ComingSoon";

export default function ServiciosPage() {
  return <ComingSoon title="Servicios" phase="Fase 2 (Onboarding + negocio + servicios + horarios)" />;
}
